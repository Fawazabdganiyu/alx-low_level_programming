#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number.
 * @n: Natural number to take square root of.
 *
 * Return: Natural square root of number or -1, if n does not have
 * a natural square root.
 */
int _sqrt_recursion(int n)
{
	if (s)
}
